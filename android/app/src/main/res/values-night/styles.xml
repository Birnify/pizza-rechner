<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dunkelmodus-Variante von values/styles.xml. Ressourcen-Qualifizierer ersetzen
         einen gleichnamigen Style komplett (kein Merge), deshalb muss die volle Definition
         wiederholt werden, nicht nur die abweichenden Werte. Einziger inhaltlicher
         Unterschied zu values/styles.xml: windowSplashScreenBackground nutzt den dunklen
         THEME_COLOR-Wert aus js/theme.js statt des hellen. -->

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="windowSplashScreenBackground">#151312</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
    </style>
</resources>
